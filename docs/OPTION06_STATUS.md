# Option 06 Status

**Approved design:** Option 06 — Modular Duty System.

**3D model status:** not finalized.

**Current repository state:** configuration scaffold + production design specification + source/license audit.

**Next gate:** detailed carrier mesh/reference review followed by production P3D, UVs, PBR materials, and Arma selections.

This branch intentionally does not claim that a finished custom 3D model has been created. The current `config.cpp` uses temporary base-game geometry for integration work until the RHD mesh is approved and exported.