name: RHD Permissions Test

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check token tools
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh --version
